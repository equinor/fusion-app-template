name: Upgrade Agent Skills
on:
  schedule:
    - cron: '0 8 * * 1-5'   # Weekday 8 AM UTC
  workflow_dispatch:         # Manual trigger allowed
permissions:
  contents: write
  pull-requests: write
jobs:
  upgrade:
    uses: equinor/fusion-skills/.github/workflows/skills-update.yml@main
